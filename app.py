from conexao import povoar_tabela, Base, engine, engine1, povoar_tabela1, Base1


# Base.metadata.drop_all(engine)
# Base.metadata.create_all(engine)
# povoar_tabela('AgentePublico')
# povoar_tabela('Empenho')
# povoar_tabela('Bens')
# Base1.metadata.drop_all(engine1)
# Base1.metadata.create_all(engine1)
povoar_tabela1('liquidacao')
